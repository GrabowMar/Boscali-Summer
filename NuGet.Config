<?xml version="1.0" encoding="utf-8"?>
<configuration>
  <!-- The solution has no package dependencies. Keeping restore offline also makes
       deterministic local builds work on machines without NuGet credentials. -->
  <packageSources>
    <clear />
  </packageSources>
</configuration>
